import { cellStyle } from "src/common/styles";
import { SxClasses } from "src/types/SxClassesType";

export const sxClasses: SxClasses = {
  value: {
    fontSize: `calc(${cellStyle.size} / 2)`
  }
};
